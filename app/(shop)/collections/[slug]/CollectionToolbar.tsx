/**
 * Placeholder for search / sort / filter / view controls.
 *
 * Renders nothing today. Kept in the tree so that when it gains content it
 * slots in without shifting the page structure or touching CollectionProducts
 * — the toolbar will drive products purely through URL search params, which
 * the page already forwards.
 *
 * Planned filter facets: brand, category, strength, size, flavor, availability.
 */
export function CollectionToolbar() {
  return null;
}
